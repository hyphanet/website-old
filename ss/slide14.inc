<h1>Misc Features</h1>
<ul>
<li>Inter-node TCP connections and secure sessions stored in a Least 
Recently Used cache to reduce setup-teardown time
<li>Data is "tunnelled" through nodes ie. the requestor starts receiving 
the data before the source has finished transmitting it
<li>Failed requests are cached
<li>Load balancing
<li>Thread-pooling
<li>Freenet Client Protocol
</ul>
